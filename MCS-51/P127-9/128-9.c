#include "reg51.h"
sbit P1_1 = P1^1;
sbit P1_0 =	P1^0;
int n=0;
int m=0;
void main(){
	P1_0=0;	  //初始输出
	P1_1=0;
	TMOD = 0x10;
	TH0	= (256-250)/256;  //初值：250us后产生溢出
	TL0 = (256-250)%256;
	EA = 1;	//开总中断
	ET0 = 1; //开定时器0中断允许位
	TR0=1;	 //开定时器中断
	while(1);	  //等待
}
timer0() interrupt 1{	//中断处理函数
 	n++;
	m++;
	if(n==4) {n=0; P1_0=~P1_0; }   //1ms到了，输出取反，输出方波
	if(m==40){m=0; P1_1=~P1_1; }	//10ms到了，输出取反，输出方波
}